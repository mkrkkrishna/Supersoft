package com.soft.dao;

/*
import org.hibernate.SessionFactory;
import org.springframework.beans.factory.annotation.Autowired;
import com.soft.model.WorkExperienceDetails;

public class WorkExperienceDetailsDAOImpl implements WorkExperienceDetailsDAO{
	
	@Autowired
    private SessionFactory sessionFactory;

	@Override
	public void saveWorkExperienceDetails(WorkExperienceDetails workExperienceDetails) {
		// TODO Auto-generated method stub
		sessionFactory.getCurrentSession().merge(workExperienceDetails);
	}
}*/
